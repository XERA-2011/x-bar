name: CI

on:
  workflow_dispatch:

jobs:
  build:
    name: Build & Verify
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build debug app
        run: |
          ./scripts/build-app.sh debug
